package PracticeInterviewProg;

// WAP to count occurrences of specific words in the sentence:"India is my country. My country is India."

public class OccurenceOfSpecificWord {

	public static void main(String[] args) {
		/*
		 * String sentence = "India is my country. My country is India.";
		 * 
		 * // Convert the sentence to lowercase and remove punctuation sentence =
		 * sentence.toLowerCase().replaceAll("[^a-z ]", "");
		 * 
		 * 
		 * // Split the sentence into words String[] words = sentence.split(" ");
		 * 
		 * // Array to keep track of counted words boolean[] counted = new
		 * boolean[words.length];
		 * 
		 * System.out.println("Word occurrences in the sentence:");
		 * 
		 * // Iterate over each word to count its occurrences for (int i = 0; i <
		 * words.length; i++) { if (!counted[i]) { String word = words[i]; int count =
		 * 1;
		 * 
		 * // Compare with remaining words in the array for (int j = i + 1; j <
		 * words.length; j++) { if (word.equals(words[j])) { count++; counted[j] = true;
		 * // Mark the word as counted } }
		 * 
		 * // Print the word and its count System.out.println(word + ": " + count); } }
		 */
		
		
    }
}


